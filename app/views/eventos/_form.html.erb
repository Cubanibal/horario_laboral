<%= form_with(model: evento) do |form| %>
  <% if evento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(evento.errors.count, "error") %> prohibited this evento from being saved:</h2>

      <ul>
        <% evento.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :fecha_inicial %>
    <%= form.datetime_select :fecha_inicial %>
  </div>

  <div class="field">
    <%= form.label :fecha_final %>
    <%= form.datetime_select :fecha_final %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
